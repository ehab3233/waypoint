# Copy to .env — the install script generates this automatically on a real deploy.
# Leave DATABASE_URL unset to run in dev mode straight from data/dataset.json (no DB needed).
DATABASE_URL=postgres://waypoint:CHANGE_ME@127.0.0.1:5432/waypoint
PORT=8080
NODE_ENV=production
